# 次元大战 — 需求待办 / 路线图（持续更新）

> 本次会话用户集中提出大量需求，按此清单逐项推进，避免遗漏。✅=已上线 main，⏳=待做。

## ✅ 已完成（本会话，均已推 main）
- UI 大改：清新玻璃配色、面板/横幅弹出动画、HP/经验平滑、按钮按下回弹（点击反馈）。
- 按 ui-ux-pro-max 技能规范：连接中加载转圈 spinner、攻击/技能就绪辉光、按压幅度调优。
- 图标全部换成 game-icons.net 真图标（CC BY 3.0，已署名）：每个职业每个技能各一张贴切图标
  （`sk_{职业}_{键}`），次元技/翻滚/攻击/背包/退出同样换真图标；按效果色着色。
- 账号系统改造（服务器）：存档主键从昵称→唯一找回码（uid `DWxxxxxxx`）；welcome 下发 code；
  老 players.json 自动迁移；昵称变可改显示名；混战/排行/签到按 uid 判定。冒烟 34/34。
- 经济/难度再平衡：升级×3、野怪金币/掉装难度×3、装备售价×10。冒烟 34/34。
- 次元技能重做（服务器逻辑，全部**持续60秒/冷却5分钟**）：
  - 修仙=**炼宝诀**（熔炼背包前5件→法宝，全属性+ Σ×等级/10）；科技=**载具冲锋**（独立护甲优先承伤+移速）；
    魔法=**召唤天使恶魔**（己方+队友增益、周围敌人减益）；赛博=**强化针剂**（全属性/技能范围/体型 ×(1+等级/10)）；
    猎人=**捕捉更难 + 吃药给宝宝回血 + 新宝宝与上一只融合(属性叠加)**。
  - 服务器下发 treasure/vehicle 状态供客户端渲染头顶法宝/载具（3D 模型为客户端待办）。冒烟 34/34。
- 怪物增强：**近战/远程混合**（约40%远程弹幕）、**每10个1个精英**（属性×2.6、攻×1.7、技能更频繁）、整体血/攻大幅提高、**等级随离出生点距离平滑增长**、**脱战回满血**、`snap` 下发 `mo.elite`、**体型随等级变大**。冒烟 34/34。
- 赛博次元技能=**强化针剂**：全属性/技能范围/**体型** ×(1+等级/10)（服务器下发 `amp`）；猎人=**捕捉更难 + 吃药奶宝宝 + 新宝宝与上一只属性融合**。冒烟 34/34。
- 狐妖(huli)接入**修仙刺客**：散图建 Standard 材质、`DW/DoubleSided` 修衣服破洞、动画混合(战斗走跑=悟空 / Idle等=狐狸)。
- 相机：怼脸看脸 + 退远压低看全身 + 「视角」复位键；光照中性化(角色不发灰发绿)。
- 商店加买药区；**动作表情**(HUD「动作」键弹 7 动作 + `emote` 联网，别的玩家也看得到)。
- 地图道具真彩：`DW/VertexColor` + 向导读回调色板原贴图 + **`EnsurePaletteImport`(sRGB+点采样+关mipmap)** 修发白；用户最新日志已确认是新版、`有第二套UV=False`，待进游戏视觉确认。

## ⏳ 待做（按优先级，已与用户约定「先登录+账号，再技能+特效」）

### ✅ 本地会话已实现（commit c405fbf，待用户进游戏截图确认/校准）
- 登录界面**左右分屏 + 左侧英雄 3D 预览**（专用相机→RenderTexture，可拖动旋转/缩放）+ 各次元 5 职业风格化称号 + `DWLogin/` 头像背景（向导⑥生成、缺失兜底）。
- 次元技能 3D 表现：炼宝诀头顶悬浮法宝、科技载具、魔法天使/恶魔、强化针剂按 `amp` 放大体型；`CastVfx` 按范围缩放、**R/大招用更大范围特效**。
- 技能名称/描述按次元×职业独立（`DWData`）。退出重进保留血量+坐标（server.js）。向导加「⑥ 修复地图道具颜色」菜单。

### A. 登录/账号收尾（待校准 + 仍缺）
- 登录左右分屏已实现，待截图校准排版/坐标；找回码自动登录联调。
- **昵称改名功能**（服务器加 `rename`，客户端入口）—— **仍未做**。

### B. 次元技能 3D 表现收尾
- 基础已实现（见上）；**精英怪 `mo.elite` 视觉区分仍待办**；各模型细节按截图打磨。

### C. 技能特效匹配（Phase 2）
- 特效要与技能**类型/范围**匹配；**R 技能用最大范围特效**（R=大招感）。
- 每个英雄技能特效尽量独立，匹配次元元素 + 职业。参考 LoL / 魔兽世界 / 笑傲江湖。

### D. 每个英雄技能独立化（Phase 2）
- 每个职业每个技能数值/手感/表现都不同，匹配该次元特色 + 职业特色。

### E. UI 美化下一轮 + 收尾
- 配色/玻璃拟态/排版/描边投影/CD 反馈（圆角已做）；UGUI 坐标真机微调。
- 地图道具发白：等用户进游戏确认；若仍白则加运行时强制点采样兜底。

## 备注
- Unity 端为盲改，UI/3D 类改动需用户截图校准。
- 改 server.js 必跑 `rm -f players.json && node smoke-test.js`（须 34/34）。
- 提交推 main（用户从 main 下载/部署）。
